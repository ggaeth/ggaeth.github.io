$(document).ready(function () {



  /*  $("#hike-modal").on("click", function () {
        $('#imagepreview').attr('src', $('#hike-image').attr('src')); // here asign the image to the modal when the user click the enlarge link
        $('#hike-modal').modal('show'); // imagemodal is the id attribute assigned to the bootstrap modal, then i use the show function
    });

    $("#trivia-moda1l").on("click", function () {
        $('#imagepreview').attr('src', $('#trivia-image').attr('src')); // here asign the image to the modal when the user click the enlarge link
        $('#trivia-modal').modal('show'); // imagemodal is the id attribute assigned to the bootstrap modal, then i use the show function
    });

    $("#liri-modal1").on("click", function () {
       // $('#imagepreview').attr('src', $('#liri-image').attr('src')); // here asign the image to the modal when the user click the enlarge link
        $('#liri-modal').modal('show'); // imagemodal is the id attribute assigned to the bootstrap modal, then i use the show function
    });

    $("#pet-modal1").on("click", function () {
      //  $('#imagepreview').attr('src', $('#pet-image').attr('src')); // here asign the image to the modal when the user click the enlarge link
        $('#pet-modal').modal('show'); // imagemodal is the id attribute assigned to the bootstrap modal, then i use the show function
    });

    $("#rps-modal1").on("click", function () {
      //  $('#imagepreview').attr('src', $('#rps-image').attr('src')); // here asign the image to the modal when the user click the enlarge link
        $('#rps-modal').modal('show'); // imagemodal is the id attribute assigned to the bootstrap modal, then i use the show function
    });

    $("#gif-modal1").on("click", function () {
      //  $('#imagepreview').attr('src', $('#gif-image').attr('src')); // here asign the image to the modal when the user click the enlarge link
        $('#gif-modal').modal('show'); // imagemodal is the id attribute assigned to the bootstrap modal, then i use the show function
    });
*/


});